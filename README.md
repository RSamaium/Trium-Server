# Get Started :

* NodeJS
* MongoDB

# Installation

`npm install -g supervisor mocha apidoc gulp`
`npm install`

# Start

`npm start`

# Tests

`npm test`

# Generate :

Create an API endpoint :

`gulp api --name {endpoint name}`

Example :

`gulp api --name media`
